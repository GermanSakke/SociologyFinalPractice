import React from 'react'
import SociologyPracticeTest from './components/SociologyPracticeTest'

export default function App(){
  return (
    <div style={{minHeight:'100vh', background:'linear-gradient(to bottom, #eef2ff, #fff)', padding:'24px'}}>
      <SociologyPracticeTest />
    </div>
  )
}
